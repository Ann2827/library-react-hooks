name: 'CI'
on:
#  push:
#    tags:
#      - '*'
  pull_request:
    branches: [ main ]
    tags:
      - '*'

jobs:
#  build:
#    #    if: github.event_name  == 'pull_request'
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - uses: actions/setup-node@v2
#        with:
#          node-version: '16'
#      - name: Install
#        run: npm ci
#      - name: Build
#        run: npm run build
#      - name: Test
#        run: npm run test
#      - name: Author
#        run: |
#          cd example
#          git config --global user.email "ann.bystrova96@mail.ru"
#          git config --global user.name "Ann Bystrova"
#      - name: Deploy
#        run: |
#          npm run predeploy
#          npm run deploy

  release:
    runs-on: ubuntu-latest
#    needs: [build]
#    env:
#      VERSION: {{ $env.version }}
#      APP_NAME: library-react-hooks
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Get Version
        run: |
          echo "VERSION=${npm run version}"

      - name: Prepare Version Tags
        run: |
          echo "MAJOR=$(echo ${VERSION} | awk -F'.' '{print $1}')" >> $GITHUB_ENV
          echo "MINOR=$(echo ${VERSION} | awk -F'.' '{print $1"."$2}')" >> $GITHUB_ENV
          echo "PATCH=$(echo ${VERSION} | awk -F'.' '{print $1"."$2"."$3}')" >> $GITHUB_ENV

#      - name: Prepare Version Tags
#        run: |
#          echo "MAJOR=$(echo ${GITHUB_REF/refs\/tags\//} | awk -F'.' '{print $1}')" >> $GITHUB_ENV
#          echo "MINOR=$(echo ${GITHUB_REF/refs\/tags\//} | awk -F'.' '{print $1"."$2}')" >> $GITHUB_ENV
#          echo "PATCH=$(echo ${GITHUB_REF/refs\/tags\//} | awk -F'.' '{print $1"."$2"."$3}')" >> $GITHUB_ENV
#
#      - name: Release
#        uses: anton-yurchenko/git-release@v4.1.2
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          args: build/*.zip
#
#      - name: Update Tags
#        uses: vweevers/additional-tags-action@v1

#      - name: Build release changelog
#        id: github_release
#        uses: mikepenz/release-changelog-builder-action@main
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          configuration: "configs/configuration.json"
#
#      - name: Create Release
##        uses: actions/create-release@v1
#        uses: ncipollo/release-action@v1
#        with:
#          tag_name: ${{ github.ref }}
#          release_name: ${{ github.ref }}
##          draft: true
#          body: ${{steps.github_release.outputs.changelog}}
#          prerelease: ${{ contains(github.ref, '-rc') || contains(github.ref, '-b') || contains(github.ref, '-a') }}
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#  generate_changelog:
#    runs-on: ubuntu-latest
#    needs: [release]
#    name: Generate changelog for master branch
#    steps:
#      - uses: actions/checkout@v1
#
#      - name: Generate changelog
#        uses: charmixer/auto-changelog-action@v1
#        with:
#          token: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Commit files
#        run: |
#          git config --local user.email "ann.bystrova96@mail.ru"
#          git config --local user.name "Ann Bystrova"
#          git add CHANGELOG.md && git commit -m 'Updated CHANGELOG.md' && echo "push=true" >> $GITHUB_ENV || echo "No changes to CHANGELOG.md"

#      - name: Push changes
##        if: env.push == 'true'
#        env:
#          CI_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        run: |
#          git push "https://Ann2827:$CI_TOKEN@github.com/$GITHUB_REPOSITORY.git" HEAD:main
