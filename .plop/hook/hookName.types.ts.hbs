export type T{{ properCase name }}Event = {};

export interface IData {
  _event(event: T{{ properCase name }}Event): void;
  on(fn: (event: T{{ properCase name }}Event) => void): void;
  reset(): void;
}

export interface I{{ properCase name }} {
  /**
   * Resets the state
   */
  reset: IData['reset'];
}
