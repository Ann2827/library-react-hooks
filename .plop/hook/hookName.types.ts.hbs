export type {{ properCase name }}DataObject = {};

export type TEvent = {};
export type TFn = (e: TEvent) => void;

/**
 * @private
 * @ignore
 */
export interface DataI {
  /** @protected **/
  _listeners: Array<TFn>;
  /** @protected **/
  _event: TFn;
  on(fn: TFn) => void): void;
  reset(): void;
}

export interface {{ properCase name }}I {
  /**
   * Resets the state
   */
  _reset: DataI['_reset'];
}
