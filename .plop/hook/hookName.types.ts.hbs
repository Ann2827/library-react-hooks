import { IData } from '../create';

export type T{{ properCase name }}State = {};

export type T{{ properCase name }}Event = {
  type: 'updated';
};
/**
 * @private
 * @ignore
 */
export interface I{{ properCase name }}Data extends IData<T{{ properCase name }}State, T{{ properCase name }}Event> {}

export interface I{{ properCase name }} {
  /**
   * Resets the state
   */
  reset: I{{ properCase name }}Data['reset'];
}
