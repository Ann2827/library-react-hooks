export type T{{ properCase name }}Event = {};
/** @private **/
export type TFn = (e: T{{ properCase name }}Event) => void;

/**
 * @private
 * @ignore
 */
export interface IData {
  /** @protected **/
  _listeners: Array<TFn>;
  /** @protected **/
  _event: TFn;
  on(fn: TFn): () => void;
  reset(): void;
}

export interface I{{ properCase name }} {
  /**
   * Resets the state
   */
  reset: IData['reset'];
}
