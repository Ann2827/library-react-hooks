import { T{{ properCase name }}Event, T{{ properCase name }}State, I{{ properCase name }}Data } from './{{ camelCase name }}.types';
import { hookCreator } from '../create';

const dataOptions = {
  hookName: '{{ camelCase name }}',
  logger: false,
};
export const logs{{ properCase name }}Enable = (): void => {
  dataOptions.logger = true;
};

export const [data, useEffectOn{{ properCase name }}] = hookCreator<T{{ properCase name }}State, T{{ properCase name }}Event, I{{ properCase name }}Data>(
  (d) => ({
    ...d,

  }),
  {  },
  dataOptions,
);
