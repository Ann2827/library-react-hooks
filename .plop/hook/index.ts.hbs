import {{ fullName prefix name }} from './{{ camelCase name }}.hook';

export { {{ fullName prefix name }} };
export type { I{{ properCase name }}, T{{ properCase name }}Event } from './{{ camelCase name }}.types';
